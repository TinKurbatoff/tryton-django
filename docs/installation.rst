============
Installation
============

At the command line::

    $ easy_install tryton-django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tryton-django
    $ pip install tryton-django
